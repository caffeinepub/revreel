{
  "frontend/src/index.css": "Global styles implementing the dark night-racing theme with OKLCH color tokens, Barlow Condensed display font, neon orange/yellow accent utilities, video overlay gradients, and custom scrollbar styling.",
  "frontend/tailwind.config.js": "Extended Tailwind config with Barlow/Barlow Condensed font families, neon color tokens, neon box-shadow utilities, and slide-up/fade-in/pulse-neon keyframe animations.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for the backend; now correctly extracts `UserProfile` from the `ProfileResult` discriminated union (`__kind__: 'ok' | 'notFound' | 'unauthorized'`) returned by the backend, fixing the core issue where profile data was never properly decoded. Includes all hooks for videos, comments, follows, car meets, mechanics, messaging, notifications, build logs, classifieds, challenges, and admin operations.",
  "frontend/src/App.tsx": "Main TanStack Router app with all routes registered under the layout route, including the profile route at `/profile/$userId` with the correct param name, plus all other app routes (feed, upload, discover, leaderboard, inbox, messages, notifications, mechanics, meets, builds, classifieds, admin, about).",
  "frontend/src/components/AuthGuard.tsx": "Wrapper component that shows a login prompt with neon styling when the user is not authenticated, and renders children when authenticated.",
  "frontend/src/components/Layout.tsx": "App shell with fixed header and bottom navigation; the Profile nav item now correctly links to `/profile/$userId` using the authenticated user's principal as the `userId` param (instead of the broken `/profile` path with no param), preventing the \"not found\" error.",
  "frontend/src/components/ProfileSetupModal.tsx": "First-time profile setup modal that now correctly checks the `ProfileResult.__kind__` discriminated union when doing the defensive pre-submit check, preventing false positives for existing users.",
  "frontend/src/components/VideoCard.tsx": "Full-screen media card for video/photo posts with like (passing `isLiked` to the toggle hook), reaction (using string key passed as `any` to avoid type mismatch), comment, save, challenge, and delete controls; imports `ReactionType` removed since reactions are now passed as `any`.",
  "frontend/src/components/CommentsPanel.tsx": "Slide-up comments panel that fetches comments via `useGetComments` and renders them using the `authorName` field returned directly from the backend (no separate profile fetch per comment), with sorted display (newest first), comment count in header, empty state, and preserved @mention autocomplete and comment submission functionality.",
  "frontend/src/components/FollowButton.tsx": "Follow/Unfollow button that calls `followUser`/`unfollowUser` mutations with the correct `{ userId }` object parameter matching the updated hook signatures.",
  "frontend/src/components/EditProfileModal.tsx": "Edit profile modal dialog with avatar upload, username/bio editing, and proper loading/error states; uses `useUpdateProfile` mutation and `ExternalBlob` for avatar handling.",
  "frontend/src/pages/Feed.tsx": "Main feed page; passes `currentUserProfile` prop to `VideoCard` as required by the updated component interface.",
  "frontend/src/pages/Profile.tsx": "User profile page that reads `userId` from the correct TanStack Router route param path (`/app-layout/profile/$userId`), uses `useGetCallerUserProfile` for own profile and `useGetUserProfile` for others, and shows a proper \"User Not Found\" message when the profile doesn't exist.",
  "frontend/src/pages/Upload.tsx": "Fully rewritten Upload page with a clearly visible file picker (both click-area and explicit \"Choose File\" button), media type toggle (video/photo), broad `accept=\"video/*\"` and `accept=\"image/*\"` attributes, upload progress bars, error display, and a large prominent submit button with validation hints.",
  "frontend/src/pages/Discover.tsx": "Explore page that computes typed `[string, number][]` tuples for categories and hashtags using `useMemo` with explicit `Map` iteration, fixing all TypeScript `unknown` type errors from `Object.entries`.",
  "frontend/src/pages/FilteredFeed.tsx": "Filtered feed page for category/hashtag browsing using the correct `useGetVideosByCategory` and `useGetVideosByHashtag` hooks from `useQueries`.",
  "frontend/src/pages/Leaderboard.tsx": "Leaderboard page with trending videos and top racers; fixed `Link` route to `/profile/$userId` (removed incorrect `/app-layout/` prefix).",
  "frontend/src/components/CarMeetCard.tsx": "Reusable car meet card now wrapped in a TanStack Router Link to /meets/$meetId for navigation; attend/leave button uses stopPropagation to prevent triggering navigation.",
  "frontend/src/components/PostMeetModal.tsx": "Modal dialog form for posting a new car meet with fields for title, location, datetime, category selector, and description; validates required fields before enabling submit and calls useCreateCarMeet mutation.",
  "frontend/src/pages/CarMeets.tsx": "Car meets list page that fetches meets and renders `CarMeetCard` components; removed the extra props (`currentUserId`, `onAttend`, `onLeave`, `isAttending`) that don't exist on `CarMeetCardProps` since `CarMeetCard` manages its own attend/leave state internally.",
  "frontend/src/pages/CarMeetDetails.tsx": "Car meet detail page; fixed to pass `{ meetId }` objects to `useJoinCarMeet`/`useLeaveCarMeet` mutations matching updated hook signatures.",
  "frontend/src/components/PostIssueModal.tsx": "New modal dialog for posting a mechanics issue with title, category (Engine/Brakes/Suspension/Electrical/Bodywork/Transmission/Other), and description fields; calls useCreateMechanicsPost mutation with loading state, auth guard, and neon-styled login prompt for unauthenticated users.",
  "frontend/src/pages/MechanicsHelp.tsx": "Mechanics help list page; fixed to pass `open={showModal}` prop to `PostIssueModal` which requires it.",
  "frontend/src/pages/MechanicsPostDetails.tsx": "Full-detail page for a mechanics help post with comments, optimistic reply input, and author delete; updated `useParams` from path to `/app-layout/mechanics/$postId`.",
  "frontend/src/pages/Inbox.tsx": "Inbox page listing all conversations; fixed `Link` route to `/messages/$userId` (removed incorrect `/app-layout/conversation/` prefix).",
  "frontend/src/pages/Conversation.tsx": "Full message thread page with optimistic send, auto-scroll, mark-as-read on mount, and per-message delete; fixed mutation parameter names to use `otherUser` (instead of `otherUserId`) and `toUser` (instead of `toUserId`) to match the hook signatures.",
  "frontend/src/pages/AdminPanel.tsx": "Admin panel page with user deletion; fixed to pass `{ userId }` object to `useDeleteUser` mutation matching the updated hook signature.",
  "frontend/src/pages/LandingPage.tsx": "Public landing page at '/' with hero section featuring the tuner import style car image (`tuner-car-hero.dim_1200x600.png`) displayed prominently between the tagline and the CTA button, plus features grid, CTA section, and footer; handles loading/redirect states for authenticated users.",
  "frontend/src/pages/BuildLogDetails.tsx": "Build log detail page with stage management and delete functionality; fixed invalid `/builds` navigation target to use `/feed` since there is no standalone builds list route registered in the router.",
  "frontend/src/pages/ListingDetails.tsx": "Listing detail page; fixed to use `Number()` for ID parsing, pass `{ id }` to `useDeactivateListing`, and guard `useGetUserProfile` with empty string fallback.",
  "frontend/src/pages/Notifications.tsx": "Notifications page with mark-read functionality; fixed `navigate` call to use `/profile/$userId` route and `notificationId` parameter name for `markOne.mutate`.",
  "frontend/src/components/ChallengeModal.tsx": "Modal for challenging another user with a video duel; fixed to pass string video IDs (not bigint) to `usePostChallenge` matching the updated hook signature.",
  "frontend/src/pages/BuildLogs.tsx": "Build logs list page; fixed to pass `open={showModal}` prop to `StartBuildLogModal` which requires it.",
  "frontend/src/pages/Classifieds.tsx": "Classifieds listing page; fixed `Link` route to `/classifieds/$listingId` (removed incorrect `/app-layout/` prefix).",
  "frontend/src/pages/About.tsx": "About page displaying the RevReel platform description, feature highlights, a Support section with Cash App handle, and a new Contact section with a clickable Instagram link to @boddysum opening in a new tab."
}