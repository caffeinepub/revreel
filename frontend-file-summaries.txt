{
  "frontend/src/index.css": "Global styles implementing the dark night-racing theme with OKLCH color tokens, Barlow Condensed display font, neon orange/yellow accent utilities, video overlay gradients, and custom scrollbar styling.",
  "frontend/tailwind.config.js": "Extended Tailwind config with Barlow/Barlow Condensed font families, neon color tokens, neon box-shadow utilities, and slide-up/fade-in/pulse-neon keyframe animations.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for the backend; defines all missing types locally (UserProfile, Badge, CarMeet, etc.) since the generated backend interface only exposes minimal types, casts actor to `any` for methods not in the generated interface, and properly types `uploadVideo` with `Variant_video_photo` mediaType parameter.",
  "frontend/src/App.tsx": "Main TanStack Router app with all routes; the root route renders a bare Outlet, the landing page at '/' has no Layout shell, and all app routes are nested under the shared `appLayoutRoute` that wraps them in the Layout shell — structure unchanged from previous version.",
  "frontend/src/components/AuthGuard.tsx": "Wrapper component that shows a login prompt with neon styling when the user is not authenticated, and renders children when authenticated.",
  "frontend/src/components/Layout.tsx": "App shell with fixed header, bottom navigation bar with 11 tabs and live unread badges; `showProfileSetup` now also checks `!isInitializing` from `useInternetIdentity` to prevent the profile setup modal from flashing before the stored identity is fully restored, ensuring the modal only appears when the user is genuinely authenticated with no profile.",
  "frontend/src/components/ProfileSetupModal.tsx": "First-time profile setup modal that now correctly passes an `ExternalBlob` avatar object along with `avatarUrl` string to `useCreateUser`, matching the updated hook signature.",
  "frontend/src/components/VideoCard.tsx": "Full-screen media card supporting both video and photo posts (via `Variant_video_photo`), renders `<img>` for photos and `<video>` for reels, with larger action buttons (w-12 h-12), like/reaction/comment/save/challenge/delete controls, and uploader profile link.",
  "frontend/src/components/CommentsPanel.tsx": "Slide-up comments panel that fetches comments via `useGetComments` and renders them using the `authorName` field returned directly from the backend (no separate profile fetch per comment), with sorted display (newest first), comment count in header, empty state, and preserved @mention autocomplete and comment submission functionality.",
  "frontend/src/components/FollowButton.tsx": "Follow/Unfollow button that calls `followUser`/`unfollowUser` mutations with the correct `{ userId }` object parameter matching the updated hook signatures.",
  "frontend/src/components/EditProfileModal.tsx": "Edit profile modal that passes `ExternalBlob` avatar and `avatarUrl` string to `useUpdateProfile`, with local type import from `useQueries` instead of the generated backend.",
  "frontend/src/pages/Feed.tsx": "Main feed page; passes `currentUserProfile` prop to `VideoCard` as required by the updated component interface.",
  "frontend/src/pages/Profile.tsx": "User profile page; imports `Badge` and all hooks from `useQueries` (not from `backend`), fixes `useUpdateAvatar` call to pass `{ avatarUrl }` object, fixes `useDeleteVideo` to pass `{ videoId }`, and uses empty string fallback for optional userId params.",
  "frontend/src/pages/Upload.tsx": "Upload page with Reel/Photo toggle selector; photo mode accepts image files (JPEG/PNG/WebP), reel mode accepts video files; passes correct `Variant_video_photo` mediaType to the backend `uploadVideo` call with upload progress feedback.",
  "frontend/src/pages/Discover.tsx": "Explore page computing unique categories and hashtags from all videos with counts, displayed as colored category tiles and hashtag pills linking to FilteredFeed.",
  "frontend/src/pages/FilteredFeed.tsx": "Filtered feed page; fetches `currentUserProfile` and passes it to `VideoCard` as required by the updated component interface.",
  "frontend/src/pages/Leaderboard.tsx": "Leaderboard page with banner image, top 10 trending videos ranked by likes, and top racers ranked by total likes across their uploads, with rank icons (crown/medal/award).",
  "frontend/src/components/CarMeetCard.tsx": "Reusable car meet card now wrapped in a TanStack Router Link to /meets/$meetId for navigation; attend/leave button uses stopPropagation to prevent triggering navigation.",
  "frontend/src/components/PostMeetModal.tsx": "Modal dialog form for posting a new car meet with fields for title, location, datetime, category selector, and description; validates required fields before enabling submit and calls useCreateCarMeet mutation.",
  "frontend/src/pages/CarMeets.tsx": "Car meets list page; replaced `CarMeetCard` import with an inline `MeetCard` component that accepts the correct props, and fixed mutation calls to pass `{ meetId }` objects.",
  "frontend/src/pages/CarMeetDetails.tsx": "Car meet detail page; fixed to pass `{ meetId }` objects to `useJoinCarMeet`/`useLeaveCarMeet` mutations matching updated hook signatures.",
  "frontend/src/components/PostIssueModal.tsx": "New modal dialog for posting a mechanics issue with title, category (Engine/Brakes/Suspension/Electrical/Bodywork/Transmission/Other), and description fields; calls useCreateMechanicsPost mutation with loading state, auth guard, and neon-styled login prompt for unauthenticated users.",
  "frontend/src/pages/MechanicsHelp.tsx": "Mechanics help list page; fixed to pass `open={showModal}` prop to `PostIssueModal` which requires it.",
  "frontend/src/pages/MechanicsPostDetails.tsx": "Full-detail page for a mechanics help post with comments, optimistic reply input, and author delete; updated `useParams` from path to `/app-layout/mechanics/$postId`.",
  "frontend/src/pages/Inbox.tsx": "New Inbox page (at /inbox) wrapped in AuthGuard that lists all conversations sorted by most recent message, showing avatar, username, message preview, relative timestamp, and a neon unread badge per conversation row.",
  "frontend/src/pages/Conversation.tsx": "Full message thread page with optimistic send, auto-scroll, mark-as-read on mount, and per-message delete; fixed mutation parameter names to use `otherUser` (instead of `otherUserId`) and `toUser` (instead of `toUserId`) to match the hook signatures.",
  "frontend/src/pages/AdminPanel.tsx": "Admin panel page with user deletion; fixed to pass `{ userId }` object to `useDeleteUser` mutation matching the updated hook signature.",
  "frontend/src/pages/LandingPage.tsx": "Public landing page at '/' with hero section featuring the tuner import style car image (`tuner-car-hero.dim_1200x600.png`) displayed prominently between the tagline and the CTA button, plus features grid, CTA section, and footer; handles loading/redirect states for authenticated users.",
  "frontend/src/pages/BuildLogDetails.tsx": "Build log detail page; fixed to use `Number()` instead of `BigInt()` for ID parsing (matching `useGetBuildLogById(number)`), pass `{ id }` to `useDeleteBuildLog`, and guard `useGetUserProfile` with empty string fallback.",
  "frontend/src/pages/ListingDetails.tsx": "Listing detail page; fixed to use `Number()` for ID parsing, pass `{ id }` to `useDeactivateListing`, and guard `useGetUserProfile` with empty string fallback.",
  "frontend/src/pages/Notifications.tsx": "Notifications page wrapped in AuthGuard that lists all notifications with icons, sender avatars, and timestamps; each notification card now uses `useNavigate` with an `onClick` handler (instead of a `Link` with an `href`) to navigate to the sender's profile page within the app, preventing the browser from misinterpreting the Principal ID as a search query.",
  "frontend/src/components/ChallengeModal.tsx": "Modal for challenging another user with a video duel; fixed to pass string video IDs (not bigint) to `usePostChallenge` matching the updated hook signature.",
  "frontend/src/pages/BuildLogs.tsx": "Build logs list page; fixed to pass `open={showModal}` prop to `StartBuildLogModal` which requires it.",
  "frontend/src/pages/Classifieds.tsx": "Classifieds list page; fixed to pass `open={showModal}` prop to `PostListingModal` which requires it."
}