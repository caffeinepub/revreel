{
  "frontend/src/index.css": "Global styles implementing the dark night-racing theme with OKLCH color tokens, Barlow Condensed display font, neon orange/yellow accent utilities, video overlay gradients, and custom scrollbar styling.",
  "frontend/tailwind.config.js": "Extended Tailwind config with Barlow/Barlow Condensed font families, neon color tokens, neon box-shadow utilities, and slide-up/fade-in/pulse-neon keyframe animations.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend and local-state interactions; adds backward-compat aliases `useJoinCarMeet`/`useLeaveCarMeet` (→ `useJoinMeet`/`useLeaveMeet`), `useGetBuildLog` (→ `useGetBuildLogDetails`), `useGetMechanicsPost` (→ `useGetMechanicsPostDetails`), `useGetListing` (→ `useGetListingDetails`), `useGetConversations` (→ `useGetInbox`), and adds the missing `useDeleteMechanicsPost` mutation; also fixes `useJoinMeet`/`useLeaveMeet` to accept a plain `string` instead of `{ meetId }`.",
  "frontend/src/App.tsx": "TanStack Router app with all routes; fixes `ProfileSetupModal` usage by passing the required `open` and `onComplete` props.",
  "frontend/src/components/AuthGuard.tsx": "Wrapper component that shows a login prompt with neon styling when the user is not authenticated, and renders children when authenticated.",
  "frontend/src/components/Layout.tsx": "App shell using `<Outlet />` for TanStack Router child route rendering; bottom nav has static-typed links for Feed/Discover/Upload/Inbox plus a separately-rendered Profile link (using `params` to avoid template-literal type errors) and a \"More\" button that opens a slide-up sheet with links to Car Meets, Build Logs, Classifieds, Mechanics Help, Leaderboard, and About.",
  "frontend/src/components/ProfileSetupModal.tsx": "First-time profile setup modal that now accepts `open: boolean` and `onComplete: () => void` props, replacing the old prop-less signature.",
  "frontend/src/components/VideoCard.tsx": "Full-screen video/photo card with muted autoplay, mute/unmute toggle, play/pause overlay, like/comment/share action buttons, CommentsPanel integration, and a clickable creator username displayed above the heart button that navigates to the creator's profile page at `/profile/$userId`.",
  "frontend/src/components/CommentsPanel.tsx": "Slide-up comments panel with a flex column layout; the input bar is now in a `shrink-0` bottom section so it stays pinned above the bottom navigation and is never hidden or overlapped by other elements.",
  "frontend/src/components/FollowButton.tsx": "Follow/Unfollow button that uses the now-exported `useGetFollowers` hook and passes `{ userId, followerId }` to the follow/unfollow mutations matching the updated hook signatures.",
  "frontend/src/components/EditProfileModal.tsx": "Edit profile modal dialog with avatar upload, username/bio editing, and proper loading/error states; uses `useUpdateProfile` mutation and `ExternalBlob` for avatar handling.",
  "frontend/src/pages/Feed.tsx": "Main feed page with snap-scroll container using IntersectionObserver (threshold 0.6) on each feed item to reliably detect the active video index, replacing the unreliable scroll-event approach; passes isActive to VideoCard based on the observed active index.",
  "frontend/src/pages/Profile.tsx": "User profile page showing avatar, username, bio, badges, and a 3-column reel grid in the Uploads tab; owner-only delete (trash) icon appears on hover over each reel thumbnail, triggers an AlertDialog confirmation, calls `useDeleteReel`, and shows a sonner toast on success or failure.",
  "frontend/src/pages/Upload.tsx": "Upload page with file picker, media type toggle, form fields, and progress bars; updated `handleSubmit` to pass `onMediaProgress`/`onThumbnailProgress` callbacks into the mutation params (instead of attaching them directly to the `ExternalBlob` before passing to the hook), and improved error handling to show user-friendly messages for auth, size, and format errors instead of raw v3 response body parse errors.",
  "frontend/src/pages/Discover.tsx": "Discover page with search and category/hashtag browsing; fixes navigate calls to always pass both `category` and `hashtag` search params (required by `filteredFeedRoute.validateSearch`).",
  "frontend/src/pages/FilteredFeed.tsx": "Filtered feed page that reads category/hashtag from URL search params via useSearch, filters all videos client-side with case-insensitive comparison, and renders playable video cards with a 'No videos found' empty state.",
  "frontend/src/pages/Leaderboard.tsx": "Leaderboard page with trending videos and top racers; restructured the content sections from a 2-column grid to stacked sections with an `AftermarketAdBanner` placed between the Trending Videos and Top Racers sections.",
  "frontend/src/components/CarMeetCard.tsx": "Car meet card component; updated imports from `useJoinCarMeet`/`useLeaveCarMeet` to `useJoinMeet`/`useLeaveMeet` and passes plain `meet.id` string to `mutate`.",
  "frontend/src/components/PostMeetModal.tsx": "Modal for posting a car meet; updated to pass only the meet fields (without `organizer`) since `useCreateCarMeet` now injects `organizer` from the current user's identity internally.",
  "frontend/src/pages/CarMeets.tsx": "Car meets list page; now injects an `AftermarketAdBanner` after every 4th car meet card using modulo arithmetic on the index, wrapping each `CarMeetCard` in a `div` to allow the banner to appear inline.",
  "frontend/src/pages/CarMeetDetails.tsx": "Car meet detail page; updated imports from `useJoinCarMeet`/`useLeaveCarMeet` to `useJoinMeet`/`useLeaveMeet` and passes plain `meetId` string to `mutate`.",
  "frontend/src/components/PostIssueModal.tsx": "New modal dialog for posting a mechanics issue with title, category (Engine/Brakes/Suspension/Electrical/Bodywork/Transmission/Other), and description fields; calls useCreateMechanicsPost mutation with loading state, auth guard, and neon-styled login prompt for unauthenticated users.",
  "frontend/src/pages/MechanicsHelp.tsx": "Mechanics help list page; now injects an `AftermarketAdBanner` after every 4th mechanics post using modulo arithmetic on the index, wrapping each post link in a `div` to allow the banner to appear inline.",
  "frontend/src/pages/MechanicsPostDetails.tsx": "Mechanics post detail page; updated imports from `useGetMechanicsPost`/`useDeleteMechanicsPost` to `useGetMechanicsPostDetails`/`useDeleteMechanicsPost` (now exported).",
  "frontend/src/pages/Inbox.tsx": "Inbox page with a tabbed interface — \"Chats\" tab shows existing conversation summaries with avatars/usernames fetched from profiles, and a new \"Following\" tab lists all followed users with their avatar, username, bio, and a \"Message\" button that navigates directly to the conversation page; both tabs handle loading and empty states with neon-styled UI.",
  "frontend/src/pages/Conversation.tsx": "DM thread page that fetches messages via useGetMessages(userId), sorts them by timestamp, renders chat bubbles with sender/receiver alignment, sends messages via useSendMessage with Principal.fromText conversion, and marks messages read on mount.",
  "frontend/src/pages/AdminPanel.tsx": "Admin panel page that checks admin status and lists all users; fixed `deleteUser.mutate` call to pass a plain `userId` string instead of an object `{ userId }`.",
  "frontend/src/pages/LandingPage.tsx": "Public landing page that now clears any persisted Internet Identity session on mount (using a `useRef` guard to run once), forcing users to log in fresh on every visit; after a successful login the user is redirected to `/feed` as normal.",
  "frontend/src/pages/BuildLogDetails.tsx": "Build log detail page; updated import from `useGetBuildLog` to `useGetBuildLogDetails`.",
  "frontend/src/pages/ListingDetails.tsx": "Listing detail page; updated import from `useGetListing` to `useGetListingDetails` and removed `any` casts since the typed `Listing` interface is now used directly.",
  "frontend/src/pages/Notifications.tsx": "Notifications page; fixed to call `useGetNotifications()` and `useMarkAllNotificationsRead()` with no arguments, and `markOne.mutate(notif.id)` with a plain number — all hooks now derive the current user internally.",
  "frontend/src/components/ChallengeModal.tsx": "Modal for challenging another user with a video duel; fixed to pass string video IDs (not bigint) to `usePostChallenge` matching the updated hook signature.",
  "frontend/src/pages/BuildLogs.tsx": "Build logs list page; now injects an `AftermarketAdBanner` after every 4th build log card using modulo arithmetic on the index, wrapping each card in a `div` to allow the banner to appear inline.",
  "frontend/src/pages/Classifieds.tsx": "Classifieds listing page with a prominent AftermarketAdBanner placed below the header and an additional ad injected after every 4th listing card.",
  "frontend/src/pages/About.tsx": "About page displaying the RevReel platform description, feature highlights, a Support section with Cash App handle, and a new Contact section with a clickable Instagram link to @boddysum opening in a new tab.",
  "frontend/src/components/PostListingModal.tsx": "Modal for posting a classified listing; updated to pass only the form fields (without `sellerId`) since `useCreateListing` now injects `sellerId` from the current user's identity internally.",
  "frontend/src/components/StartBuildLogModal.tsx": "Modal for starting a build log; updated to pass only the build fields (without `authorId`) since `useCreateBuildLog` now injects `authorId` from the current user's identity internally.",
  "frontend/src/components/AftermarketAdBanner.tsx": "Reusable clickable ad banner component for eBay aftermarket car parts that opens the exact affiliate URL in a new tab, styled with the app's dark/neon theme, includes a \"Sponsored\" label, banner image, and neon-orange CTA text."
}