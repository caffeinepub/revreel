{
  "frontend/src/index.css": "Global styles implementing the dark night-racing theme with OKLCH color tokens, Barlow Condensed display font, neon orange/yellow accent utilities, video overlay gradients, and custom scrollbar styling.",
  "frontend/tailwind.config.js": "Extended Tailwind config with Barlow/Barlow Condensed font families, neon color tokens, neon box-shadow utilities, and slide-up/fade-in/pulse-neon keyframe animations.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for the backend, now extended with two admin hooks: `useIsAdmin` (queries `actor.isAdmin()`) and `useDeleteUser` (mutation calling `actor.deleteUser()` with Principal conversion and cache invalidation).",
  "frontend/src/App.tsx": "Main app router restructured so the root route renders a bare Outlet, the landing page at '/' has no Layout shell, and all app routes (/feed, /profile, /upload, etc.) are nested under a shared layout route that wraps them in the Layout shell.",
  "frontend/src/components/AuthGuard.tsx": "Wrapper component that shows a login prompt with neon styling when the user is not authenticated, and renders children when authenticated.",
  "frontend/src/components/Layout.tsx": "App shell with fixed header and bottom navigation bar now featuring 8 tabs including a new DMS (MessageCircle) tab linking to /inbox, with a live neon unread count badge sourced from useGetInbox.",
  "frontend/src/components/ProfileSetupModal.tsx": "Modal dialog for first-time users to set their racer name, bio, and an optional profile picture — adds a clickable circular avatar preview with camera overlay that opens a file picker, converts the selected image to a data URL, and passes it as `avatarUrl` to the `createUser` mutation on submit.",
  "frontend/src/components/VideoCard.tsx": "Full-screen video card component with auto-play/pause, mute/unmute toggle, like/comment/share actions, and owner-only delete. Fixes the audio bug by introducing `isMutedRef` that stays in sync with the `isMuted` prop, so the `isActive` play effect always reads the current (non-stale) muted value; also adds graceful autoplay fallback to muted if the browser blocks audio autoplay.",
  "frontend/src/components/CommentsPanel.tsx": "Slide-up comments panel showing existing comments with timestamps, and an authenticated input form to post new comments via addComment mutation.",
  "frontend/src/components/FollowButton.tsx": "Follow/Unfollow button that guards against unauthenticated use, calls followUser/unfollowUser mutations with the userId string directly (Principal conversion now handled inside the mutation), and shows a loading spinner during pending state.",
  "frontend/src/components/EditProfileModal.tsx": "Dialog for editing the current user's profile (username, bio, and avatar) — adds a clickable circular avatar preview with camera overlay that opens a file picker, converts the selected image to a data URL, and passes it (or the existing `avatarUrl`) to the `updateProfile` mutation on submit.",
  "frontend/src/pages/Feed.tsx": "Main feed page with For You/Following tabs and vertical snap-scroll; changes the default mute state from `true` to `false` (unmuted) so videos play with audio by default, while VideoCard handles browser autoplay policy by falling back to muted if needed.",
  "frontend/src/pages/Profile.tsx": "User profile page unchanged in structure but now shows a neon-styled MSG button (MessageCircle icon linking to /messages/$userId) next to the Follow button when viewing another authenticated user's profile.",
  "frontend/src/pages/Upload.tsx": "Video upload form page with file selection for video and thumbnail, title/description inputs, 8-category picker, hashtag tag input, upload progress bar, and ExternalBlob conversion — fixes the `thumbnail`/`videoUrl` key names to `thumbnailBlob`/`videoBlob` to match the `useUploadVideo` mutation signature.",
  "frontend/src/pages/Discover.tsx": "Explore page computing unique categories and hashtags from all videos with counts, displayed as colored category tiles and hashtag pills linking to FilteredFeed.",
  "frontend/src/pages/FilteredFeed.tsx": "Vertical snap-scroll video feed filtered by category or hashtag; updated `useParams` from path to `/app-layout/filter/$type/$value` and destructures `type`/`value` directly.",
  "frontend/src/pages/Leaderboard.tsx": "Leaderboard page with banner image, top 10 trending videos ranked by likes, and top racers ranked by total likes across their uploads, with rank icons (crown/medal/award).",
  "frontend/src/components/CarMeetCard.tsx": "Reusable car meet card now wrapped in a TanStack Router Link to /meets/$meetId for navigation; attend/leave button uses stopPropagation to prevent triggering navigation.",
  "frontend/src/components/PostMeetModal.tsx": "Modal dialog form for posting a new car meet with fields for title, location, datetime, category selector, and description; validates required fields before enabling submit and calls useCreateCarMeet mutation.",
  "frontend/src/pages/CarMeets.tsx": "Car Meets page with category filter pills and a new Upcoming/Past date toggle segmented button; filters and sorts meets by both category and date, with combined filter state maintained locally.",
  "frontend/src/pages/CarMeetDetails.tsx": "Full-detail page for a single car meet with organizer info, date/location, attendee list, and attend/leave button; updated `useParams` from path to `/app-layout/meets/$meetId`.",
  "frontend/src/components/PostIssueModal.tsx": "New modal dialog for posting a mechanics issue with title, category (Engine/Brakes/Suspension/Electrical/Bodywork/Transmission/Other), and description fields; calls useCreateMechanicsPost mutation with loading state, auth guard, and neon-styled login prompt for unauthenticated users.",
  "frontend/src/pages/MechanicsHelp.tsx": "New Mechanics Help list page showing all mechanics issue posts sorted newest-first as glassmorphism cards with category badges, author username, comment count, and relative timestamps; includes a FAB for authenticated users and a PostIssueModal.",
  "frontend/src/pages/MechanicsPostDetails.tsx": "Full-detail page for a mechanics help post with comments, optimistic reply input, and author delete; updated `useParams` from path to `/app-layout/mechanics/$postId`.",
  "frontend/src/pages/Inbox.tsx": "New Inbox page (at /inbox) wrapped in AuthGuard that lists all conversations sorted by most recent message, showing avatar, username, message preview, relative timestamp, and a neon unread badge per conversation row.",
  "frontend/src/pages/Conversation.tsx": "Full message thread page with optimistic send, auto-scroll, mark-as-read on mount, and per-message delete; fixed mutation parameter names to use `otherUser` (instead of `otherUserId`) and `toUser` (instead of `toUserId`) to match the hook signatures.",
  "frontend/src/pages/AdminPanel.tsx": "Admin panel page wrapped in AuthGuard that checks `useIsAdmin` to gate access, now displays the authenticated user's Principal ID as a selectable/copyable diagnostic box (with a copy-to-clipboard button) on both the \"Access Denied\" screen and the authorized admin view, plus the existing delete-user form with confirmation dialog and inline feedback.",
  "frontend/src/pages/LandingPage.tsx": "Public landing page at '/' with a full-screen hero section (background image, RevReel logo, tagline, neon-styled \"Join RevReel\" CTA button), a features grid, a bottom CTA section, and a footer. Unauthenticated users can click \"Join RevReel\" to trigger Internet Identity login; authenticated users are automatically redirected to '/feed'.",
  "frontend/src/pages/BuildLogDetails.tsx": "Detail page for a single build log showing car info, build stages timeline, and owner controls (add stage, delete); updated `useParams` from path to `/app-layout/builds/$buildId`.",
  "frontend/src/pages/ListingDetails.tsx": "Detail page for a classified listing showing image, price, condition, seller info, and deactivate button for the seller; updated `useParams` from path to `/app-layout/classifieds/$listingId`."
}